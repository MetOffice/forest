# Service
kind: Service
metadata:
  name: {{ .Values.manager.name }}
spec:
  ports:
    - port: {{ .Values.manager.name }}
      targetPort: {{ .Values.manager.name }}
      protocol: TCP
  selector:
    app: {{ .Values.manager.name }}