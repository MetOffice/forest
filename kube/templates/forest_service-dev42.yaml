# Service
kind: Service
metadata:
  name: {{ .Values.managerdev42.name }}
spec:
  ports:
    - port: {{ .Values.managerdev42.port }}
      targetPort: {{.Values.managerdev42.port}}
      protocol: TCP
  selector:
    app: {{ .Values.managerdev42.name }}