version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/forest
hooks:
  BeforeInstall:
    - location: server/install_packages.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: server/start_docker.sh
      timeout: 300
      runas: root
